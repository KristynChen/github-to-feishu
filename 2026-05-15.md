# 2026-05-15 aiCAD Benchmark / Judge Reliability 日報

## 1. 今日核心進展

今天主要推進了 aiCAD 的 CI benchmark 基礎設施，重點是把 VLM-as-a-Judge 從「人工看 dashboard」升級成「artifact-driven、可測、可 gate、可追 failure queue」的工作流。

### Judge Reliability Gate Artifact
- 新增 `latest_judge_gate_result.json`，把多個 VLM judge report 合併成 CI 可讀的 gate artifact。
- 支援同一 model / case 的 rerun 覆蓋，避免單 case JSON 失敗污染整體模型評估。
- Dashboard 已改為讀 artifact，不再只依賴手寫靜態資料。

### Qwen 3.5 Plus 成為目前最佳 VLM-as-Judge
- `qwen3.5-plus` full daily set 經過 JSON extraction 修正與 case rerun 後：
  - Raw accuracy: `0.8`
  - Adjudicated accuracy: `1.0`
  - JSON valid rate: `1.0`
  - API success rate: `1.0`
  - Judge score: `198/200`
- 結論：目前 `qwen3.5-plus` 是最適合作為大規模 VLM-as-Judge 的候選模型。

### Kimi K2.6 問題定位
- Kimi K2.6 API call 成功，但 JSON valid rate 長期為 `0%`。
- 測試 streaming / non-streaming / strict JSON prompt 後，仍會輸出大量 reasoning prose，無法穩定只回 JSON。
- 結論：Kimi K2.6 不是 API 問題，而是 structured-output contract 不可靠，暫時不適合作為 CI judge。

---

## 2. Full Run Stress Mode

新增 `run_all_benchmarks.py --mode stress` / `--stress`。

```bash
python3 benchmarks/run_all_benchmarks.py --stress --continue-on-error
```

Stress mode 現在會串起：
1. Interaction Routing summary
2. Full Suite Replay
3. Model Arena Cost Summary
4. Corpus stress benchmark
5. Main Agent Spec Completeness
6. Failure Queue

最新結果：
- Aggregate summary: `passed`
- Stress cases: `135`
- Corpus stress status: `warning`
- Warning 原因：`real_user_*` case id 同時存在於 visual benchmark 和 routing prompts，屬於跨資料集共用來源，不阻斷 full run。

---

## 3. Main Agent Spec Completeness 壓測

新增 `latest_main_agent_spec_completeness.json`，專門壓測 Main Agent 是否能把 CAD request 轉成足夠完整的 structured requirements。

最新基線：
- Total cases with requirements contract: `45`
- Passed: `29`
- Failed: `16`
- Pass rate: `0.6444`

關鍵觀察：
- 明確尺寸類 case 表現較好：`explicit_dimensions` pass rate `0.9259`
- real user / parametric cases 明顯較弱：`parametric` pass rate `0.4444`
- 弱點集中在 feature preservation、material/finish、long context、多語 prompt。

---

## 4. Failure Queue 建立

新增 `latest_failure_queue.json`，把所有 artifact 的失敗項統一轉成可分配 queue item。

目前 queue 狀態：
- Open items: `52`
- High priority: `34`
- Medium priority: `18`

Owner breakdown:
- `interaction_agent`: `18`
- `main_agent_spec_extraction`: `16`
- `benchmark_triage`: `15`
- `image_model`: `1`
- `model_candidate_evaluation`: `2`

這讓後續修復可以直接按 owner / priority 分派，而不是人工翻多份 benchmark report。

---

## 5. Adjudication Ruleset 固化

今天把人工判斷的 image defect 經驗固化成可測 ruleset。

新增 `adjudication_rules.py`：
- `dimension_conflict`
- `unrequested_topology`
- `missing_critical_dimension`

規則語義：
- 如果 raw label 是 HQ，但 judge block 了它，且 evidence 命中上述規則，則不計為 judge false positive。
- 這類 case 會被改判為 `adjudicated_generation_defect`，也就是 image model defect。

已固化的 regression cases：
- `daily_phone_stand_hq_gpt_image2`
  - 問題：front retaining lip 在需求 / Front View 是 `10mm`，但 Section A-A 標 `8mm`
  - 命中：`dimension_conflict`
- `daily_toothbrush_holder_hq_gpt_image2`
  - 問題：圖中加入未要求的 `6X drain holes`，且 drip tray 關鍵尺寸缺失 / 衝突
  - 命中：`unrequested_topology`、`missing_critical_dimension`

驗證：
```bash
uv run pytest tests/test_adjudication_rules.py tests/test_vlm_judge_fallback_runner.py
```

結果：`15 passed`

---

## 6. 今日產出 Artifacts

- `main-agent-benchmark/results/reports/latest_judge_gate_result.json`
- `main-agent-benchmark/results/reports/latest_benchmark_run.json`
- `main-agent-benchmark/results/reports/full_run_stress_latest.json`
- `main-agent-benchmark/results/reports/latest_main_agent_spec_completeness.json`
- `main-agent-benchmark/results/reports/latest_failure_queue.json`

---

## 7. 明日建議推進

1. 先處理 `main_agent_spec_extraction` 的 16 個 failure，尤其是 real user parametric prompts。
2. 把 `latest_failure_queue.json` 接入 dashboard，讓 dashboard 顯示 owner / priority / next_action。
3. 把 `latest_judge_gate_result.json` 作為 CI gate 的正式輸入，避免 dashboard 和 CI 使用不同資料源。
4. 針對 Kimi / DeepSeek structured output 做更小的單 case prompt lab，但暫時不把它們列入主 judge candidate。
